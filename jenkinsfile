pipeline {
  agent any
    environment {
      MY_CREDENTIAL = credentials('test-hola-mundo')
    }
  stages {
    stage('Primer paso') {
      steps {
        sh('echo ${MY_CREDENTIAL}')
      }
    }
  }
}

