pipeline {
  agent any
    environment {
      MY_CREDENTIAL = credentials('test-hola-mundo')
    }
  stages {
    stage('Primer paso') {
      steps {

        script {
          echo "La credencial es: ${MY_CREDENTIAL}"
        }
      }
    }
  }
}

